from flet import ColorScheme, Colors

scheme = ColorScheme(
    primary=Colors.ORANGE,
    secondary=Colors.ORANGE_ACCENT,
    primary_container=Colors.ORANGE,
    secondary_container=Colors.ORANGE_ACCENT,
    primary_fixed=Colors.ORANGE,
    secondary_fixed=Colors.ORANGE,
)
